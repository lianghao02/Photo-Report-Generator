# HANDOFF

## 目前狀態
可交付

## 本輪目標
1. 診斷並修復使用者回報之「Ctrl / Shift 多選照片失效」問題。
2. 逐項實測並確認使用者指定之 16 項核心操作功能（選取操作、移動與換序、歷史復原、縮放與大圖燈箱）。
3. 將多選測試納入自動化 E2E 測試套件，確保品質防歸退。

## 已完成
1. **根本原因診斷與修復**：
   - 診斷出卡片上的 `beginCardPointerDrag(index, event)`（`onpointerdown`）執行時機先於 `onclick`（`handleCardClick`）。
   - 原邏輯在未按住 Ctrl/Shift 判斷下，誤將其他卡片設為 `selected = false` 並覆寫 `lastSelectedIndex`，導致隨後觸發的 `click` 事件中 Ctrl 切換與 Shift 範圍多選被破壞。
   - 在 `index.html` 的 `beginCardPointerDrag` 前端加入防護：若按住 `ctrlKey`、`metaKey` 或 `shiftKey`，即刻返回，不啟動拖曳亦不干預選取狀態，讓事件完整由點擊處理常式處理。
2. **16 項核心操作全面驗證（全數 PASS）**：
   - **選取操作**：
     - 多選照片（`Ctrl + 點選`）：✅ 正常（獨立切換點擊卡片選取狀態）
     - 連續範圍選取（`Shift + 點選`）：✅ 正常（由基準起迄選取整段範圍）
     - 矩形框選（舞台空白處拖曳）：✅ 正常（框選範圍內批次選取）
     - 全選 / 全不選（點擊工具列「全選」）：✅ 正常（批次切換 `selectAllPhotos`）
     - 單張編輯（雙擊縮圖展開）：✅ 正常（展開編輯面板並聚焦說明輸入框）
   - **移動與換序**：
     - 切換目前照片（`↑` `↓` `←` `→`）：✅ 正常（非輸入框焦點下快速切換當前選取卡片）
     - 前後換序移動（`Ctrl + ← / →`）：✅ 正常（陣列前後換位並寫入歷史）
     - 跨列上下移動（`Ctrl + ↑ / ↓`）：✅ 正常（依版型行數跨列換位並寫入歷史）
     - 移至開頭 / 結尾（`Ctrl + Home / End`）：✅ 正常（直接置頂或置底）
   - **歷史復原**：
     - 復原動作（`Ctrl + Z`）：✅ 正常（回復至前一操作狀態）
     - 重做動作（`Ctrl + Y`）：✅ 正常（重做已復原動作）
   - **縮放與大圖燈箱**：
     - 開啟大圖燈箱（`Space / Enter`）：✅ 正常（非輸入焦點下開啟鑑識燈箱）
     - 燈箱縮放檢視（滾輪 / `+` `-`）：✅ 正常（0.35x ~ 4.0x 游標錨點縮放）
     - 燈箱平移拖曳（放大時按住左鍵拖曳）：✅ 正常（雙層舞台隔離座標平移）
     - 燈箱旋轉照片（`R` / `Shift+R`）：✅ 正常（順/逆旋轉，即時同步主畫布，延遲歷史提交）
     - 適合視窗還原（`0`）：✅ 正常（重設 100% 置中）
     - 關閉任何彈窗（`ESC`）：✅ 正常（關閉大圖燈箱及其他 Modal）
3. **自動化測試擴充與品管通過**：
   - `tests/e2e/photo-report.spec.js` 擴充 `Ctrl + 點選` 與 `Shift + 連續範圍選取` 測試斷言。
   - `npm test`：全部通過 (4/4)
   - `npm run test:e2e`：全部通過
   - `pwsh -NoProfile -File ./scripts/qa.ps1`：通過

## 刻意未修改
- 未改動 Word、PDF、Excel、ZIP 匯出資料結構。
- 未改動拖曳排序的核心演算法，僅阻斷多選修飾鍵觸發之拖曳誤判。

## 尚未完成
無

## 驗證結果
### 已執行
- `npm test`：通過 (4/4)
- `npm run test:e2e`：通過 (包含 Ctrl/Shift 多選、燈箱全功能、雙擊展開)
- `pwsh -NoProfile -File ./scripts/qa.ps1`：通過
- `scratch/verify-all-features.js`：實測 16/16 全部 PASS

### 尚未驗證
無阻斷性未驗證項目。

### 已知風險
- 當雙擊卡片展開編輯器後，焦點會進入說明輸入框；此時鍵盤方向鍵用於文字游標移動（避免干擾文字輸入），點擊卡片或空白處失焦後方向鍵即可恢復切換照片。

## Git 狀態
- Commit：未提交（待提交）
- Push：否
- Working Tree：Modified
- Branch: main

## 下一步
- 提交 Git Commit。
