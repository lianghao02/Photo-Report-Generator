# 📝 更新日誌 (CHANGELOG)

## 🔮 下一版本預計規劃 (Roadmap v2.3.0)
- 🗺️ **EXIF GPS 經緯度反查與軌跡標記**：
  - 自動讀取照片 EXIF GPS 座標與拍攝方位角，於工作台卡片提供地圖預覽捷徑。
  - 支援一鍵反查行政區路段地址自動帶入「採證地點」。
- 🔖 **OpenXML 自訂防偽浮水印與公文密級印記**：
  - 匯出 Word 清冊時支援於背景嵌入半透明斜向浮水印（如「公務機密」、「偵查不公開」、「案號自訂」）。
- 📥 **既有清冊 Word (`.docx`) 逆向反向匯入**：
  - 支援選取先前產出的清冊 `.docx`，純前端逆向解析提取照片與表格資料回工作台進行二次編輯。
- 🔍 **純前端離線 WebAssembly OCR 輔助辨識**：
  - 整合輕量離線 OCR 引擎，一鍵辨識照片中車牌、招牌或監視器時間文字並智慧帶入說明。

---

## 🏆 v2.2.1 (2026-09-01)
- **穩定拖曳排序**：改用指標拖曳與浮動插入提示；卡片順序僅在放開時更新，不會因游標移動改變舞台版面。
- **桌面版狀態清理**：拖曳在舞台外放開、操作取消或視窗失焦時，會立即清除「移動中…」提示與浮動插入槽。
- **工作區彈性配置**：桌面版新增左右欄可拉伸分隔線，記住設定寬度；窄螢幕維持上下排列。
- **欄位輸入修正**：日期與時間改為離開欄位後統一格式化，避免 Windows 輸入時發生數字重複。

## 🏆 v2.2.0 (2026-08-31)
- **安全操作與歷程管理**：新增最多 50 步 Undo／Redo（`Ctrl + Z` / `Ctrl + Y`），支援刪除、清空、旋轉、拖曳／鍵盤排序、流水號與批次資料套用；不複製原始影像檔以嚴格控制記憶體。
- **工作台搜尋與篩選**：新增即時搜尋、未填說明／地點與已選照片篩選、指定位置跳轉，以及縮圖延遲解碼。
- **PDF 清冊輸出**：新增離線 `jsPDF` 匯出，依上下兩張、左右兩張、橫式三張版型排版；中文文字由本機 Canvas 高解析嵌入 PDF。
- **大量照片韌性**：載入與 Word／PDF／ZIP 匯出提供可取消按鈕；縮圖啟用延遲解碼，降低一次顯示數百張照片的負擔。
- **版本檢核與一鍵更新**：以 `version.txt` 驗證 Tauri 建置版本一致性；頂部導覽列新增版本徽章與 GitHub Release 非同步檢查更新機制。

## 🏆 v2.1.2 (2026-08-26)
- **時間與日期智慧遮罩 (Masked Input)**：
  - 採證／監視器時間支援純數字遮罩輸入：輸入 4 碼自動轉為 `HH:MM`、輸入 6 碼自動轉為 `HH:MM:SS`，保留手動輸入冒號相容性。
  - 右側編輯器與 DataGrid 試算表新增**個別照片採證日期欄位**（中華民國曆遮罩，如輸入 7 碼 `1130826` 自動轉為 `113/08/26`）；若留空則自動沿用左側全域日期。
  - 三大 Word 版型（上下兩張、左右兩張、橫式三張）及 Excel 匯出／匯入全面支援個別自訂日期優先輸出。
- **工作台鍵盤整組同步移動與全向方向鍵**：
  - 重構 `moveSelectedBy` 演算法為「提取整組選取群 → 計算插入錨點 → 重組陣列」，徹底解決多選照片移動時後方照片先動、前方慢一步的不協調感。
  - 新增 `Ctrl + ↑`（整組向上移動一列）與 `Ctrl + ↓`（整組向下移動一列），動態依畫布欄數同步移動。
- **現場跡證說明向下填滿保護防護**：
  - 執行說明「向下填滿」時，智慧跳過已有資料的格子（`desc !== ''`），僅填補空白照片，避免意外覆寫已撰寫的跡證說明。

- **工作台互動革新 (PaperSwitch 手感)**：
  - 支援 `Ctrl + 滑鼠滾輪` 在 90–400px 之間平滑連續調整縮圖尺寸，一般滾輪正常捲動頁面。
  - 支援從畫布空白處滑鼠拖曳框選多張照片；支援 `Ctrl` 多選與 `Shift` 連續區間選取。
  - 支援多選後直接拖曳任一卡片進行整組排序；支援 `Ctrl + ← / →` 前後移動一格與 `Home / End` 移至首尾。
- **大量圖片架構優化**：
  - 改用 Object URL 保留原圖，預覽採用 320px 低記憶體縮圖，大幅降低記憶體壓力。
  - 支援 500 張／2GB 載入提示防護；載入、Word 匯出與 ZIP 匯出皆具備進度條與非同步讓步機制。
  - 刪除或清空照片時保留 Object URL 供 Undo／Redo 使用；超出歷程上限或捨棄重做分支後，才釋放未再參照的資源。
- **左側固定工作區版面**：
  - 介面升級為左側固定操作面板（案件資訊、版型設定、Word/ZIP/Excel 匯出按鈕常駐），右側為照片畫布與可收合之批次編輯器。
  - 解決海量照片時匯出按鈕被推擠至頁面底部的問題。
- **明確批次編輯與流水號保護**：
  - 右側編輯器可針對多選照片批次填寫時間、地點、說明，按「將已填欄位套用至已選照片」明確寫入；支援「空白欄位也覆寫」選項。
  - 照片編號嚴格受保護，批次編輯絕不覆寫既有流水號。
- **第三方資源 100% 離線化**：
  - Tailwind CSS、FontAwesome、`docx.js`、`jszip`、`xlsx`、`FileSaver` 全部收錄至 `vendor/` 目錄，斷網公務環境 100% 正常運作。
- **真實瀏覽器自動化驗證**：
  - 導入 Playwright 針對滾輪手勢、框選、排序與匯出進行實測，修復 `LineRuleType` 匯出 ReferenceError。

## 🏆 v2.0.0 (2026-08-24)
- **架構重構**：徹底淘汰 VBA 巨集與本機 Office 依賴，全面重構成純前端現代化單頁 Web 應用程式 (SPA)。
- **版型升級**：完整繼承並內建三大經典 Word 版型（上下兩張 A4 直式、左右兩張 A4 直式雙欄、橫式三張 A4 橫式三欄）。
- **時間模式**：推出專為公務現場量身打造之四大智慧時間填寫模式（檔名時間正則解析、基準時間步進遞增、向下填滿、剪貼簿多行貼上）。
- **批次工具**：整合智慧流水號產生器、EXIF 方向自動校正、Canvas 300DPI 列印最佳化壓縮與雙視圖切換（卡片預覽 vs 試算表 DataGrid）。
- **格式輸出**：採用 `docx.js` 於前端記憶體直接組裝原生標準 OpenXML `.docx` 檔案，100% 零巨集安全警示；支援 Excel 清單雙向匯出與匯入。
- **歷史封存**：將舊版 `清冊編輯VBA-0820.xlsm` 與舊範本完整封存於 `legacy_vba/` 目錄，達成零污染與完整歷史回溯。

## 🏆 v1.1.2 (2026-08-17)
- **操作指南**：補齊 Office 需求、巨集入口、範本相對位置與移機打包方式。
- **用途釐清**：明確區分 `index.html` 展示頁與 `清冊編輯.xlsm` 實際工具。
- **相容性敘述**：移除絕對穩定與未定義等級宣稱，改列實際人工檢查項目。

## 🏆 v1.1.1 (2026-08-13)
- **編碼修復**：重建 CHANGELOG 為 UTF-8 無 BOM，移除歷史亂碼。
- **文件**：README 補充 Office、巨集安全性與相容性限制，移除不可驗證效能宣稱。

## 🏆 v1.1.0 (2026-08-10)
- **核心架構**：整理現場照片清冊範本與 D 槽專案路徑。
- **環境文件**：同步全域開發憲法與專案編號結構。
