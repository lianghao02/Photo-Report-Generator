# 實作計畫

## 目標與驗收條件

讓單次載入數百張照片時，避免以 Base64 常駐原圖造成記憶體膨脹，並維持三種 Word 表格版型、Excel 匯入與 ZIP 匯出的既有功能。

- 原圖預覽改用 Object URL，縮圖限制在 320px 內。
- 載入與匯出有可見進度，並保持介面可重新繪製。
- 清空或刪除照片時釋放所有 Object URL。
- 達 500 張或總檔案大小達 2GB 前顯示確認提示，不靜默中止。
- DOCX 與 ZIP 仍逐張處理圖片，版型與資料欄位不變。

## 不做範圍

- 不導入後端、資料庫或 Tauri 原生檔案串流。
- 不改變報表版型、圖片壓縮尺寸與既有編輯資料格式。
- 不保證瀏覽器在裝置實體記憶體不足時可處理任意大小的圖片集。

## 現況與限制

- 現況在載入時以 FileReader 轉成完整 Base64，數百張原圖會大幅增加記憶體占用。
- `docx.js` 組裝仍需在匯出時將每張壓縮圖轉為 ArrayBuffer，無法完全無記憶體消耗。
- 專案已有未提交的離線資源與安全修補變更，必須保留。

## 已確認決策

- 使用瀏覽器 Object URL 保留原圖參考，並另外生成小型縮圖 Object URL。
- 載入預設允許 500 張／2GB；超過時由使用者確認後才繼續。
- 匯出流程維持逐張處理，額外加入批次進度回報與事件迴圈讓步。

## 工作清單

- [x] 新增載入與匯出進度 UI、數量與容量確認｜已完成靜態檢查；本環境未提供 Browser 控制工具
- [x] 改用 Object URL 與縮圖資料模型，並在刪除／清空時釋放資源｜已確認 createObjectURL 與 revokeObjectURL 呼叫點
- [x] 調整 DOCX／ZIP 匯出為批次進度流程｜JavaScript 語法檢查通過
- [x] 更新使用說明與驗證紀錄｜qa.ps1 與資源產製通過
- [x] 工作台改為 Ctrl＋滾輪連續縮放縮圖（90–400px），移除 2／3／4 欄切換
- [x] 補強照片框選、多選拖曳與鍵盤排序操作
- [x] 改善已選照片的說明／地點批次編輯，支援明確套用與空白覆寫
- [x] 執行語法、品質檢核與可用範圍內的 UI 驗證｜node 語法檢查、qa.ps1 與 git diff --check 通過；無 Browser 存取權限

## 風險與因應

- 部分瀏覽器無法解碼 HEIC：沿用既有影像解碼錯誤提示。
- 大型 DOCX 本身仍可能耗用大量記憶體：匯出期間回報進度，並建議超大批次分冊。
- Object URL 未釋放會造成記憶體洩漏：集中建立與釋放方法，刪除與清空皆呼叫。

## 驗證紀錄

- 2026-08-26：`scripts/prepare-web.ps1` 成功產製與同步本機前端資源。
- 2026-08-26：擷取內嵌 JavaScript 並以 `node --check` 驗證，無語法錯誤。
- 2026-08-26：`scripts/qa.ps1` 與 `git diff --check` 通過。
- 2026-08-26：Antigravity 透過 Playwright 執行完整瀏覽器 UI 實測，解除限制：
  - ✅ **Ctrl＋滾輪連續縮放**：實測 235px → 250px → 220px，滾輪縮放與重設按鈕 100% 正常。
  - ✅ **畫布空白處框選 (Marquee)**：滑鼠拖曳框選 4 張照片卡片，框選多選狀態完全正確。
  - ✅ **鍵盤排序**：`Ctrl + ArrowRight`、`End`（移至末尾）、`Home`（移至開頭）排序演算法運作正常。
  - ✅ **批次編輯與套用**：多選照片後批次填寫地點與說明，照片編號受保護不被覆寫，套用至已選張數邏輯精確。
  - ✅ **修復 LineRuleType ReferenceError**：修復 `exportDocx()` 中解構漏掉 `LineRuleType` 導致匯出報錯的 Bug。
  - ✅ **Word 報表產出驗證**：實測成功產出 `.docx`，內含 2 個獨立表格、圖片等比不變形置中、中繼段落精準維持固定行高 5 點 (`line: 100`)。
  - ✅ **打包腳本健全**：`build-portable.ps1` 整合調用 `prepare-web.ps1`，確保離線 vendor 資源同步打包。

## 剩餘問題

- 目前功能與 UI 互動皆已自動化實測驗證通過，隨時可進行 Git Commit 提交或產出正式安裝包。

## 2026-08-26 工作台操作改善

### 目標與驗收條件

- 照片區不再以固定欄數控制密度；Ctrl＋滑鼠滾輪可在 90–400px 之間連續調整縮圖寬度。
- 保留一般滾輪捲動，並提供目前縮圖尺寸與重設操作。
- 可從畫布空白處框選照片；既有 Ctrl／Shift 多選與多選拖曳排序繼續可用。
- 可用 Ctrl＋左右方向鍵將已選照片前後移動，Home／End 直接移至首尾。
- 批次編輯清楚顯示影響張數，僅在明確按下套用時才寫入已選照片，且可選擇以空白清除欄位。

## 2026-08-27 v2.2.0 工作台安全與輸出升級

### 目標與驗收條件

- 提供至少 50 步的 Undo／Redo，涵蓋照片排序、旋轉、刪除、批次欄位寫入與清空；不複製原始影像資料。
- 工作台提供明確的搜尋／篩選、跳轉與批次操作提示，數百張照片仍可快速定位。
- 新增等比例圖片、三種既有版型相容的 PDF 清冊下載。
- 版本號以 `version.txt` 為建置驗證來源，桌面版與網頁資源版本一致；更新檢查保有離線失敗提示與安全下載連結。
- 大量照片時縮圖延遲解碼、渲染讓步、匯出可取消，並維持既有 Object URL 釋放機制。
- 更新 README、CHANGELOG、版本設定，執行 QA 與實際網頁測試後提交並推送 `origin/main`。

### 不做範圍

- 不新增雲端上傳、後端服務或自動下載／覆蓋桌面程式。
- 不將原始照片寫入 Undo 歷程或長期瀏覽器儲存空間。
- 不變更既有 Word 三種表格欄位與列印規格。

### 工作清單

- [x] 建立輕量 Undo／Redo 歷程與快捷鍵｜50 步上限、分支捨棄資源釋放與 JavaScript 語法檢查通過
- [x] 加入工作台搜尋、篩選、跳轉與批次操作 UX｜靜態 DOM／事件綁定檢查通過；本工作階段 Browser 本機頁面政策禁止互動實測
- [x] 新增 PDF 清冊匯出與本機離線資源｜`jsPDF` 已內建並完成 v2.2.0 Tauri 正式建置；待使用者以實際案件照片進行列印視覺確認
- [x] 加強大量照片載入／縮圖／匯出取消機制｜取消旗標、批次讓步與 Object URL 生命週期檢查通過
- [x] 統一 v2.2.0 版本、更新機制與建置驗證｜`version.txt`、`package.json`、Tauri/Cargo 皆為 2.2.0，桌面正式建置通過
- [ ] 更新 README／CHANGELOG 並完成 QA、提交、推送｜文件與 QA 已完成，待 Git 提交與推送

### 驗證紀錄

- 2026-08-27：`scripts/prepare-web.ps1` 通過，確認 `web/vendor/jspdf.umd.min.js` 已納入離線前端資產。
- 2026-08-27：擷取內嵌 JavaScript 後執行 `node --check` 通過；`scripts/qa.ps1` 與 `git diff --check` 通過。
- 2026-08-27：`scripts/build-portable.ps1` 通過，產出 `照片清冊產生器_2.2.0_x64-setup.exe`（2.33 MB）及 `照片清冊產生器_2.2.0_x64_portable.zip`（2.59 MB）。
- 2026-08-27：Codex 內建 Browser 因本機 `file://` URL 存取政策拒絕，未以替代路徑繞過；PDF 版面仍需使用實際案件照片手動確認。
